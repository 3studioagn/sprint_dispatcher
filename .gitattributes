# Normaliza line endings: armazena LF no repo, deixa o checkout decidir.
* text=auto eol=lf

# Arquivos binários — nunca aplicar conversão.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.zip binary
*.exe binary
*.msi binary
*.dll binary
*.node binary
*.woff binary
*.woff2 binary

# Lock files — preservar como gerados pelo gerenciador.
pnpm-lock.yaml -text
