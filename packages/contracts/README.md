# @sprint/contracts

Tipos, schemas Zod e helpers compartilhados do Sprint Dispatcher.

Library interna, source-first (consumida diretamente em TypeScript pelos apps
Electron via bundler). Não publica em registry público.

## API pública

Será documentada conforme cada item do backlog (BL-C1-\*) for implementado.
